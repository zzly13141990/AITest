# 应用图标文件夹

此文件夹用于存放应用图标文件。

## 添加应用图标

1. 准备一个 ICO 格式的图标文件（推荐尺寸 256x256）
2. 将文件命名为 `icon.ico`
3. 放在此文件夹中
4. 重新运行打包命令

## 图标格式要求

- **格式**: ICO
- **尺寸**: 256x256（或包含多种尺寸）
- **文件名**: `icon.ico`

如果没有提供图标，Electron Builder 将使用默认图标。
